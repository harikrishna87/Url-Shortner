<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie-edge">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
        <title>URL Shortener</title>
        <style>
          body {
            font-family:Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
            padding: 0px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center; 
            margin-top: 40px;
            background: #1b052f;
          }
          
          .container h1 {
            color:#1b052f;
            font-size: 45px;
            text-align: center;
          }
          
          label {
            display: block;
            margin-bottom: 10px;
            margin-left: 40px;
            margin-top: 35px;
            color: #1b052f;
            font-size: 22px;
          }
          .container {
            width:75%;
            height: auto;
            min-height:420px;
            padding: 10px;
            border-radius: 20px;
            box-shadow: 0px 0px 11px 4px rgba(36, 20, 59, 0.25);
            background: -webkit-linear-gradient(to right, #cc8ffb, #600f89);  
            background: linear-gradient(to right, #ce8ffb, #600f89);
            margin: auto;
          }
          table a, td {
            color: #fff;
          }
          .credit {
            height: 3vh;
            text-align: center;
            margin-top: 80px;
            font-size: 23px;
            font-family:'Times New Roman', Times, serif;
            color: #250d3c;
            margin-bottom: 15px;
          }
        </style>
    </head>
    <body>
        <div class="container">
          <h1>URL Shortener</h1>
          <label for="url">Enter Url Below :</label>
          <form action="/shortUrls" method="POST" class="my-4 form-inline">
            <label for="fullUrl" class="sr-only">Url</label>
            <input required placeholder="https://www.example.com/long-url" type="url" name="fullUrl" id="fullUrl" class="form-control col mr-1">
            <button class="btn btn-success" type="submit">Shorten</button>
          </form>
          
          <table class="table table-striped table-responsive">
            <thead>
              <tr>
                <th>Full URL</th>
                <th>Short URL</th>
                <th>Clicks</th>
              </tr>
            </thead>
            <tbody>
              <% shortUrls.forEach(shortUrl => { %>
                <tr>
                  <td><a href="<%= shortUrl.full %>"><%= shortUrl.full %></a></td>
                  <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
                  <td><%= shortUrl.clicks %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
          <div class="credit">Link shortener - Designed By : Hari || Full Stack Web Development Intern at Slash Mark </div>
        </div>
      </body>
</html>